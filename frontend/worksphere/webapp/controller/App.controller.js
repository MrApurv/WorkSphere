sap.ui.define([
    "sap/ui/core/mvc/Controller"
], function (Controller) {
    "use strict";

    return Controller.extend(
        "com.apurv.worksphere.controller.App",
        {
            onInit: function () {
                // Application-level initialization can be added here later.
            }
        }
    );
});